import { ChecknumberDirective } from './checknumber.directive';

describe('ChecknumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ChecknumberDirective();
    expect(directive).toBeTruthy();
  });
});
